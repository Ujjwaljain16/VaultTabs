# Backend Environment Variables
# Copy to .env and fill in values

# ─── Required ──────────────────────────────────────────────────────────────────

# Postgres connection (for Neon, append ?sslmode=require)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# JWT secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_64_char_hex_secret_here

# ─── CORS ──────────────────────────────────────────────────────────────────────

# Comma-separated list of allowed ORIGINS in production
# Include your Vercel PWA URL and the chrome-extension:// origin
# Get extension ID from: chrome://extensions after loading the unpacked build
ALLOWED_ORIGINS=https://vaulttabs.vercel.app,chrome-extension://YOUR_EXTENSION_ID_HERE

# ─── HTTPS ─────────────────────────────────────────────────────────────────────

# Set to "false" on Railway / Render / Fly.io where SSL terminates at the proxy
# Set to "true" (default) for local dev with mkcert certificates
HTTPS_ENABLED=false

# Your public IP/hostname (used to find the right mkcert cert file in local dev)
# Leave empty on production
PUBLIC_IP=

# ─── Optional ──────────────────────────────────────────────────────────────────

NODE_ENV=production
PORT=3000
