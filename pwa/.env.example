# PWA Environment Variables (set these in Vercel dashboard Settings → Environment Variables)
# For local dev, copy to .env.local

# Internal API URL (your Railway / Render backend)
NEXT_PUBLIC_API_URL=https://your-backend.railway.app/api/v1
